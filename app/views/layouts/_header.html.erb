<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
    	<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
      	</button>
      	<a class="navbar-brand" href="#">Hydea</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
	    <ul class="nav navbar-nav">

	      <li>
	      	<%= link_to 'Ideas', ideas_path %>
	      </li>
	      <li>
	      	<%= link_to 'Users', users_path %>
	      </li>
	    </ul>
      <ul class='nav navbar-nav navbar-right'>
        <li><%= t :greeting %></li> <!-- TESTIKENTTÄ LOKALISAATIOTA VARTEN -->
        <li><%= link_to_unless I18n.locale == :en, "English", language_english_path %></li>
        <li><%= link_to_unless I18n.locale == :fin, "Finnish", language_finnish_path %></li>
        <li><%= link_to_unless I18n.locale == :swe, "Svenska", language_swedish_path %></li>
	    <% if not session[:user_id].nil? %>
			    <li><%= link_to User.find(session[:user_id]).name, User.find(session[:user_id])%></li>
		    <li><%= link_to 'Logout', logout_path, method: :delete %></li>
		<% else %>
			<li><%=  link_to 'Login', haka_auth_new_path %></li>
		<% end %>
    </ul>
	</div>
  </div>
</nav>
